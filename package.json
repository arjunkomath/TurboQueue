{
  "name": "turbo-queue",
  "scripts": {
    "dev:api": "bun run --hot src/api.ts",
    "dev:worker": "bun run --hot src/worker.ts"
  },
  "dependencies": {
    "hono": "^4.7.5",
    "ioredis": "^5.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}